using Bookish.DbModels;
using Microsoft.EntityFrameworkCore;
using System;
using System.Collections.Generic;

namespace Bookish.Queries
{
    public class BookQuery
    {
        //public static List<BookDbModel>  Query()
        //{
        //    var context = new LibraryContext();

        //    var books = context.Books
        //                      .FromSqlRaw("Select * from Books where Id = 1").ToListAsync();
        //    books.Start();
        //    books.Wait();

        //    Console.WriteLine(books.Result[0].Title);

        //    return books.Result;


        //}
    }
}
